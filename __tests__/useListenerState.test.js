import React from "react";

test("it sets state like normal useState");
test("it listens for state changes on the entire object");
test("it stops listening for changes");
test("it listens for changes once");
// TODO test that it listens for changes on a particular object.path.like.this